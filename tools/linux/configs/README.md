# configs/ (3 конфига):
 - .bashrc — кастомный шелл с алиасами, функциями (extract, bak, mkcd), git-ветка в промпте
 - nginx.conf — production-конфиг: reverse proxy, rate limiting, SSL, gzip, security headers
 - crontab.txt — примеры cron-задач с объяснениями
