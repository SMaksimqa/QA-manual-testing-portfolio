# scripts/ (4 bash-скрипта):
 - system_info.sh — диагностика системы с прогресс-барами CPU/RAM/Disk
 - backup.sh — инкрементальные бэкапы через rsync с ротацией
 - log_analyzer.sh — анализ nginx/auth/syslog логов (top IP, коды ответов, брутфорс)
 - user_manager.sh — создание/удаление пользователей + аудит безопасности
